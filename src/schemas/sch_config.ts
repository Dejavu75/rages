export type MySQLConfig = {
    host: string
    user: string
    password: string
    database: string
    port: string
  }

  export type servingConfig = {
    port: string
    port_local:string
    port_ssl: string
  }